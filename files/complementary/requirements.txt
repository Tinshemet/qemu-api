requests>=2.31.0
psutil>=5.9.0
rich>=13.0.0
fastapi>=0.111.0
uvicorn>=0.29.0
httpx>=0.27.0
cryptography>=42.0.0  # .grgn agent-file encryption (Fernet) — see shared/grgn_sign.py
windows-curses>=2.3.0; sys_platform == "win32"  # curses TUI (client) — Unix has it built-in
